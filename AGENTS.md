# Agent Entry Point

Start here before making changes.

Primary context location:
- `./_AI_SYSTEM/README.md`

Operating rules:
- Treat `./_AI_SYSTEM/` as the canonical project context for AI agents.
- Use `./_AI_SYSTEM/PROJECT_INDEX.md` to find the current product truth, architecture docs, environment references, and verification surfaces.
- Do not commit secrets, `.env*`, or local Vercel state.
- Before large changes, review `replit.md` and the documents indexed from `./_AI_SYSTEM/PROJECT_INDEX.md`.
- When adding durable AI-readable project context, prefer placing it in `./_AI_SYSTEM/` or linking it there with generic filenames.
